get:
  description: Use this URL to determine registration status.
  security:
    - bearerAuth: []
  responses:
    '200':
      description: get:/customer/registration response
      content:
        application/json:
          schema:
            type: object
          examples:
            response:
              summary: Example 1
              externalValue: 'https://raw.githubusercontent.com/vloschiavo/powerwall2/master/samples/21.44-foogod/running/customer.registration.json'
    '401':
      $ref: '../../spec.yaml#/components/responses/Unauthorized'